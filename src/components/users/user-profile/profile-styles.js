// src/components/users/user-profile/profile-styles.js
//
// Shared class strings for the profile pages so the paper-and-ink language
// (mono micro-labels, bordered card sheets) stays consistent across the
// page header, tabs and both profile sheets.

// Mono uppercase micro-label, the landing page's eyebrow voice.
export const microLabel =
  "font-mono text-[10px] font-bold uppercase tracking-tight text-muted-foreground";

// A bg-card bordered sheet, the profile page's content surface.
export const sheet = "rounded-2xl border border-border bg-card p-5 sm:p-6";
